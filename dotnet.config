# .NET 10 opt-in to the Microsoft.Testing.Platform runner. xunit.v3 4.x no
# longer supports the VSTest path, so `dotnet test` fails without this.
[dotnet.test.runner]
name = "Microsoft.Testing.Platform"
